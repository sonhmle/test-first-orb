description: >
  Sample description
# What will this job do?
# Descriptions should be short, simple, and clear.

executor: default

parameters:
  to:
    type: env_var_name
    default: TO_NAME
    description: "Hello to whom?"
steps:
  - greet:
      to: << parameters.to >>
